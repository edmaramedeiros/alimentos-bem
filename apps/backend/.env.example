# Copie para .env (ou configure como variáveis de ambiente reais no Render) e preencha.
# Usado a partir do Marco 1 (auth) em diante.

SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080

# String de conexão Postgres (Neon - usar o endpoint "pooled")
DATABASE_URL=jdbc:postgresql://localhost:5432/edmara_alimentos
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres

JWT_SECRET=troque-por-um-segredo-longo-e-aleatorio

WHATSAPP_SERVICE_URL=http://localhost:3001
WHATSAPP_SERVICE_API_KEY=troque-por-uma-chave-compartilhada

CORS_ALLOWED_ORIGIN=http://localhost:8081
