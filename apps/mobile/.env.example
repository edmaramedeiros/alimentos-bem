# Copie para .env e preencha. Variáveis EXPO_PUBLIC_* ficam expostas no bundle
# do cliente (web e Android) - nunca coloque segredos aqui, só a URL da API.

EXPO_PUBLIC_API_BASE_URL=http://localhost:8080
